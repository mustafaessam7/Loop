package com.example.data.local

object InitialData {
    suspend fun seed(db: LoopDatabase) {
        // Zero-State Initialization:
        // Absolutely zero pre-populated catalog items, services, fake invoices, or mock cashbox transactions.
        // The workshop starts completely fresh and empty.
    }
}
